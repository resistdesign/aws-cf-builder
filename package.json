{
  "name": "aws-cf-builder",
  "main": "index.js",
  "author": "Resist Design",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "workspaces/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach run build",
    "release": "read -p 'Type (major|minor|patch): ' TYPE; yarn workspaces foreach version $TYPE"
  },
  "prettier": {
    "printWidth": 150,
    "semi": true,
    "singleQuote": true,
    "trailingComma": "es5",
    "useTabs": false
  },
  "devDependencies": {
    "prettier": "^2.3.2"
  }
}
